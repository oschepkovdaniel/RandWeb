#pragma once

#include <iostream>
#include <string>
using namespace std;

class LinkUtils
{
public:

	static string RemoveNumbers(string);
	static string RemoveLetters(string);
	static string ResolveAdress(string);
};